<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="olga.maslova.outerspacemanager">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".Activities.SignUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.MainActivity" />
        <activity android:name=".Activities.VueGeneraleActivity" />
        <activity android:name=".Activities.BuildingActivity"
        />
        <activity android:name=".Activities.ChantierActivity" />
        <activity android:name=".Activities.ShipDetailActivity" />
        <activity android:name=".Activities.BuildingDetailActivity"

            />
        <activity android:name=".Activities.FleetActivity" />
        <activity android:name=".Activities.GalaxieActivity" />
        <activity android:name=".Activities.AttackActivity" />
        <activity android:name=".Activities.ShowReportActivity" />
        <activity android:name=".Activities.ResearchActivity"></activity>
    </application>

</manifest>